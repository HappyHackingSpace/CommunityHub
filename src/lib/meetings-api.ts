// Backward compatibility wrapper - Use @/features/meetings/services/meetings.service instead
// This file exists only for backward compatibility with existing code

export { meetingsService as meetingsApi } from '@/features/meetings/services/meetings.service';
